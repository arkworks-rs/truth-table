{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "test-planner",
			"type": "shell",
			"command": "cargo test --package planner --lib --all-features --quiet"
		},
		{
			"label": "planner-tests",
			"type": "shell",
			"command": "cargo test --package planner --lib --all-features"
		}
	]
}